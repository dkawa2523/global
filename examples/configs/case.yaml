include: base_case.yaml

case:
  name: icp_bias_cf4_demo
  description: >
    Main full-duration demonstration case. Uses include/override so the user can
    manage shared defaults separately from the case-specific files.

files:
  recipe: recipe.yaml
  output_dir: ../outputs/icp_bias_cf4_demo
